services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: hideaway
      POSTGRES_PASSWORD: hideaway
      POSTGRES_DB: hideaway57
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
